# عقد مراحل وأصول الهوية

لا تنشئ ملف JSON أو manifest. هذا المرجع يحدد ما يجب أن يتسلمه التاجر فقط.

## المرحلة الأولى: معاينات الاختيار

- أنشئ الآن بالضبط `concept-1.png` و`concept-2.png` و`concept-3.png` عبر ثلاث عمليات أحادية متتابعة، مرفق صورة مستقلًا لكل لوحة، 1536x1024 أو أقرب معاينة مدعومة مع ذكر المقاس الفعلي خارج الصورة.
- لا تُرسل أكثر من بطاقة `<single-image-prompt>` في العملية الواحدة، ولا تبدأ التالية حتى يظهر مرفق الحالية. PNG المرسومة بالكود أو SVG أو لقطة أو قصاصة ليست تصورًا صالحًا.
- النص المرئي الوحيد داخل كل تصور هو اسم المتجر العربي الحرفي. المنتجات سياق تصميمي فقط؛ لا وصف نشاط ولا سلوغان ولا عناوين ألوان أو خطوط أو استخدامات ولا رقم اتجاه داخل الصورة.
- تتباعد المناهج بين علامة لفظية بمساحة سلبية، وعلامة مجردة قابلة للملكية، ونظام طباعي تعبيري؛ لا تكون إعادة تلوين ولا حلًا حرفيًا متوقعًا من فئة المنتج.
- هذه المعاينات ليست ثلاثية التسليم النهائي ولا تُفحص بفاحص الأصول. لا تسأل سؤال الاختيار حتى تظهر المرفقات الثلاثة؛ صورة مركبة واحدة أو أسماء الملفات لا تكفي.
- عند الرفض، استبدلها بثلاثة اتجاهات جديدة لا تعيد الاتجاه المرفوض.

## المرحلة الثانية: بعد موافقة صريحة

| القدرة | الكتالوج | الشعار | الأيقونة | الألوان النصية |
|---|---|---|---|---|
| توليد/تحرير صورة فعلي مع حفظ وفحص | `brand-catalog.png` 1536x1024 RGB أو RGBA 8-bit غير متداخل؛ الشفافية اختيارية | `logo-ar.png` 1024x256 RGBA 8-bit غير متداخل؛ الأركان الأربعة شفافة و1% `alpha=0` | `store-icon.png` 32x32 بالشروط نفسها | Primary وSecondary HEX بعد الصور |

- صيغة التسليم الوحيدة هي PNG؛ يرفض الفاحص SVG في كل البيئات والحالات.
- يلزم ثلاثة ملفات مستقلة فعلًا؛ الكتالوج هو كامل لوحة الاتجاه المختار، وليس صورة مجمعة جديدة أو قصاصة منها.
- الشعار صورة جديدة بعملية إنشاء مستقلة على لوحة كاملة من الصفر باستخدام الكتالوج مرجعًا بصريًا، وليس قصًا أو استخراجًا أو لقطة شاشة أو تغيير مقاس منطقة منه.
- الأيقونة صورة جديدة بعملية إنشاء مستقلة على لوحة مربعة كاملة من الصفر باستخدام علامة الشعار الجديد مرجعًا بصريًا؛ يجوز تصغير هذا الأصل المستقل إلى 32x32، ولا يجوز قصه من الكتالوج أو الشعار.
- يجب أن تبقى هندسة الرمز وأسلوبه وألوانه متطابقة مع الاتجاه المعتمد بين الأصول.
- الصور نفسها مولدة بصريًا داخل المحادثة وليست ملفات PNG منشأة بالكود؛ يمنع Python/Pillow وSVG كطريقة لصنعها. يجوز للكود فقط فحص الأصل المولد أو تصديره/تصغيره كاملًا بلا رسم أو إضافة أو إعادة تركيب.
